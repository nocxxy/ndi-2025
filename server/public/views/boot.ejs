<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Windows - Démarrage</title>
    <link rel="stylesheet" href="<%= staticUrl('/stylesheets/style.css') %>" />
    <style>
        .boot-screen {
            background-color: #000000;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            width: 100vw;
            color: white;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .windows-logo {
            width: 100px;
            height: 100px;
            margin-bottom: 60px;
        }

        /* Modern Windows Logo (Four squares) */
        .win-square {
            fill: #00a4ef;
        }

        /* Spinner */
        .loader {
            position: relative;
            width: 60px;
            height: 60px;
        }

        .loader div {
            box-sizing: border-box;
            display: block;
            position: absolute;
            width: 48px;
            height: 48px;
            margin: 6px;
            border: 4px solid #fff;
            border-radius: 50%;
            animation: loader 1.2s cubic-bezier(0.5, 0, 0.5, 1) infinite;
            border-color: #fff transparent transparent transparent;
        }

        .loader div:nth-child(1) { animation-delay: -0.45s; }
        .loader div:nth-child(2) { animation-delay: -0.3s; }
        .loader div:nth-child(3) { animation-delay: -0.15s; }

        @keyframes loader {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="boot-screen">
        <svg class="windows-logo" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 88 88">
            <path class="win-square" d="M0 12.402l35.687-4.86.016 34.423-35.67.216zm35.687 37.634l-.016 34.775L0 80.235l.033-30.427zM43.813 10.68L88 4.395v37.567l-44.187.254zm0 39.522L88 49.936v38.669l-44.187-6.072z"/>
        </svg>
        
        <div class="loader">
            <div></div><div></div><div></div><div></div>
        </div>
    </div>

    <script>
        // Redirect to windows simulation after 4 seconds
        setTimeout(() => {
            window.location.href = "<%= url('/windows') %>";
        }, 4000);
    </script>
</body>
</html>
