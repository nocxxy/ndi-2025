<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pause Sport Express</title>
    <link rel="stylesheet" href="<%= staticUrl('/stylesheets/style.css') %>">
    <script src="<%= staticUrl('/javascripts/message-bus.js') %>"></script>
    <script src="<%= staticUrl('/javascripts/sport-app.js') %>"></script>
    <script defer src="<%= staticUrl('/javascripts/alpine.js') %>"></script>
    <style>
        [x-cloak] { display: none !important; }
        .card-hover { transition: all 0.3s ease; }
        .card-hover:hover { transform: translateY(-4px); box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12); }
        .active-scale:active { transform: scale(0.98); }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 via-blue-50 to-cyan-50 min-h-screen font-sans text-gray-900" x-data="sportApp">

    <!-- Header -->
    <header class="bg-white shadow-sm sticky top-0 z-50 border-b border-gray-100">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 bg-gradient-to-br from-blue-600 to-blue-700 rounded-xl flex items-center justify-center shadow-lg">
                    <span class="text-white font-black text-xl">D</span>
                </div>
                <div>
                    <h1 class="font-black text-xl tracking-tight">DECATHLON</h1>
                    <p class="text-blue-600 text-xs font-semibold">Pause Sport Express</p>
                </div>
            </div>
            <div class="hidden md:flex items-center space-x-2 bg-blue-50 px-4 py-2 rounded-full border border-blue-100">
                <span class="text-blue-700 text-sm font-semibold">⚡ 2 minutes</span>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <div class="container mx-auto px-4 py-12 max-w-5xl">

        <!-- Welcome Screen -->
        <div x-show="currentScreen === 'welcome-screen'" x-transition.opacity>
            <div class="bg-white rounded-3xl shadow-xl overflow-hidden border border-gray-100">
                <div class="relative bg-gradient-to-br from-blue-600 via-blue-500 to-cyan-400 p-16 text-center">
                    <div class="text-8xl mb-6 animate-bounce">🏃‍♂️</div>
                    <h2 class="text-5xl font-black text-white mb-4 tracking-tight">Votre Pause Sport Idéale</h2>
                    <p class="text-xl text-blue-50 mb-8">En 5 questions, trouvez le programme parfait pour vous</p>
                    <div class="flex justify-center gap-12 text-white">
                        <div class="text-center"><div class="text-4xl font-black">5</div><div class="text-sm text-blue-100 font-medium">Questions</div></div>
                        <div class="text-center"><div class="text-4xl font-black">2</div><div class="text-sm text-blue-100 font-medium">Minutes</div></div>
                        <div class="text-center"><div class="text-4xl font-black">1</div><div class="text-sm text-blue-100 font-medium">Programme</div></div>
                    </div>
                </div>
                <div class="p-10">
                    <button @click="startQCM()" class="active-scale w-full bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white font-black py-6 px-8 rounded-2xl transition-all shadow-lg hover:shadow-xl text-xl tracking-wide">
                        🚀 C'EST PARTI !
                    </button>
                </div>
            </div>
        </div>

        <!-- QCM Screen -->
        <div x-show="currentScreen === 'qcm-screen'" x-transition.opacity>
            <div class="bg-white rounded-3xl shadow-xl overflow-hidden border border-gray-100">
                <div class="bg-gradient-to-r from-blue-600 to-cyan-500 p-8">
                    <div class="flex justify-between items-center mb-4">
                        <span class="text-white font-black text-lg">Question <span x-text="currentQuestionIndex + 1"></span>/5</span>
                        <div class="bg-white/20 backdrop-blur-sm px-5 py-2 rounded-full border border-white/30">
                            <span class="text-white font-black text-sm"><span x-text="progressPercent"></span>%</span>
                        </div>
                    </div>
                    <div class="w-full bg-white/20 rounded-full h-3 overflow-hidden backdrop-blur-sm">
                        <div class="bg-gradient-to-r from-yellow-400 to-yellow-300 h-3 rounded-full transition-all duration-500 shadow-lg" :style="`width: ${progressPercent}%`"></div>
                    </div>
                </div>
                <div class="p-10">
                    <h3 class="text-2xl font-bold text-gray-900 mb-6" x-text="currentQuestion.question"></h3>
                    <div class="space-y-3">
                        <template x-for="option in currentQuestion.options" :key="option.value">
                            <button @click="selectAnswer(currentQuestion.id, option.value)" class="w-full text-left p-5 rounded-xl border-2 border-gray-200 hover:border-blue-500 hover:bg-blue-50 transition-all flex items-center space-x-4 group shadow-sm hover:shadow-md">
                                <span class="text-4xl" x-text="option.icon"></span>
                                <span class="text-gray-800 group-hover:text-blue-700 font-medium text-lg" x-text="option.label"></span>
                            </button>
                        </template>
                    </div>
                </div>
            </div>
        </div>

        <!-- Results Screen -->
        <div x-show="currentScreen === 'results-screen'" x-transition.opacity>
            <div class="bg-white rounded-3xl shadow-xl overflow-hidden border border-gray-100">
                <div class="bg-gradient-to-br from-green-500 via-emerald-500 to-teal-500 p-12 text-center">
                    <div class="text-8xl mb-4">🎉</div>
                    <h2 class="text-4xl font-black text-white mb-3 tracking-tight">Votre Programme Est Prêt !</h2>
                    <p class="text-green-50 text-lg font-medium">Personnalisé selon vos besoins</p>
                </div>
                
                <div class="p-10 space-y-8">
                    <!-- Profile Summary -->
                    <div>
                        <h3 class="text-xl font-bold text-gray-900 mb-4">📋 Votre Profil Sportif</h3>
                        <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
                            <template x-for="(val, key) in userProfile">
                                <div class="bg-white rounded-lg p-3 text-center border border-gray-100">
                                    <div class="text-xs text-gray-500 mb-1 capitalize" x-text="key"></div>
                                    <div class="font-bold text-gray-800 capitalize" x-text="val"></div>
                                </div>
                            </template>
                        </div>
                    </div>

                    <!-- Exercise -->
                    <div class="bg-gradient-to-br from-green-50 to-teal-50 rounded-2xl p-6 border-2 border-green-200">
                        <div class="flex items-center space-x-4 mb-6">
                            <div class="text-6xl" x-text="resultExercise.image"></div>
                            <div class="flex-1">
                                <h3 class="text-2xl font-black text-gray-900" x-text="resultExercise.name"></h3>
                                <div class="flex items-center space-x-2 mt-1">
                                    <span class="bg-green-500 text-white px-3 py-1 rounded-full text-sm font-bold" x-text="'⏱️ ' + resultExercise.duration"></span>
                                </div>
                            </div>
                        </div>
                        
                        <div class="mb-6 p-4 bg-blue-500 text-white rounded-xl shadow-lg">
                            <p class="font-bold" x-text="resultExercise.visual"></p>
                        </div>

                        <div class="mb-6">
                            <h4 class="font-bold text-gray-900 mb-4 text-lg flex items-center"><span class="mr-2">📝</span> Instructions détaillées</h4>
                            <div class="space-y-3">
                                <template x-for="(instruction, idx) in resultExercise.instructions">
                                    <div class="flex items-start space-x-3 bg-white p-4 rounded-xl shadow-sm">
                                        <span class="flex-shrink-0 w-8 h-8 bg-gradient-to-br from-green-500 to-teal-500 text-white rounded-full flex items-center justify-center text-sm font-bold shadow-md" x-text="idx + 1"></span>
                                        <p class="text-gray-700 flex-1 leading-relaxed" x-text="instruction"></p>
                                    </div>
                                </template>
                            </div>
                        </div>

                        <div class="p-4 bg-gradient-to-r from-yellow-100 to-orange-100 rounded-xl border-l-4 border-yellow-500">
                            <p class="font-bold text-gray-900" x-text="resultExercise.tips"></p>
                        </div>
                    </div>

                    <!-- Products -->
                    <div class="bg-white rounded-xl p-4 shadow-md border border-gray-100">
                        <div class="flex items-center mb-4">
                            <span class="text-2xl mr-2">🛒</span>
                            <div>
                                <h3 class="text-lg font-bold text-gray-900">Produits recommandés</h3>
                                <p class="text-xs text-gray-500">Pour votre entraînement</p>
                            </div>
                        </div>
                        <div class="space-y-3">
                            <template x-for="product in resultProducts" :key="product.id">
                                <div @click="window.open(product.link, '_blank')" class="cursor-pointer bg-gray-50 rounded-xl overflow-hidden hover:shadow-lg transition-all active:scale-98 border border-gray-200 hover:border-blue-400 flex">
                                    <div class="w-24 h-24 flex-shrink-0 bg-white flex items-center justify-center">
                                        <img :src="product.image" :alt="product.name" class="w-full h-full object-cover">
                                    </div>
                                    <div class="flex-1 p-3 flex flex-col justify-between">
                                        <div>
                                            <h4 class="font-bold text-gray-900 text-sm mb-1 line-clamp-2" x-text="product.name"></h4>
                                            <p class="text-xs text-gray-600 line-clamp-1" x-text="product.description"></p>
                                        </div>
                                        <div class="flex items-center justify-between mt-2">
                                            <span class="text-lg font-bold text-blue-600" x-text="product.price"></span>
                                            <div class="flex items-center space-x-1 text-blue-600 text-xs font-semibold">
                                                <span>Voir</span>
                                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" /></svg>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </template>
                        </div>
                    </div>

                    <div class="pt-4">
                        <button @click="restart()" class="active-scale w-full bg-gradient-to-r from-gray-800 to-gray-900 hover:from-gray-900 hover:to-black text-white font-black py-5 px-8 rounded-2xl transition-all shadow-lg hover:shadow-xl">
                            🔄 Nouveau Programme
                        </button>
                    </div>
                </div>
            </div>
        </div>

    </div>
</body>
</html>
