<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Snake Game</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 20px;
    }

    .game-container {
      text-align: center;
    }

    #score {
      font-size: 28px;
      font-weight: bold;
      color: #fff;
      margin-bottom: 25px;
      text-shadow: 0 0 10px rgba(52, 199, 89, 0.5);
      letter-spacing: 2px;
    }

    .box_snake {
      background: linear-gradient(145deg, #2a2a3e, #1f1f2e);
      border-radius: 20px;
      padding: 30px;
      box-shadow: 
        0 10px 40px rgba(0, 0, 0, 0.6),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
      border: 2px solid rgba(52, 199, 89, 0.3);
    }

    #snake_table {
      display: flex;
      flex-direction: column;
      gap: 0;
      border: 4px solid #34c759;
      background: #0a0a0a;
      padding: 0;
      border-radius: 12px;
      box-shadow: 
        0 0 30px rgba(52, 199, 89, 0.4),
        inset 0 0 20px rgba(0, 0, 0, 0.8);
      overflow: hidden;
    }

    .ligne {
      display: flex;
      gap: 0;
    }

    .cellule {
      width: 25px;
      height: 25px;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #111;
      border: 0.5px solid rgba(52, 199, 89, 0.1);
      font-size: 18px;
      transition: all 0.1s ease;
      position: relative;
    }

    .cellule::before {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle, rgba(52, 199, 89, 0.05) 0%, transparent 70%);
      opacity: 0;
      transition: opacity 0.3s;
    }

    .cellule:hover::before {
      opacity: 1;
    }

    .game-over-screen, .win-screen {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.95);
      color: white;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      z-index: 1000;
      animation: fadeIn 0.3s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .game-over-screen h1 {
      font-size: 64px;
      margin: 0 0 20px 0;
      color: #ff3b30;
      text-shadow: 0 0 20px rgba(255, 59, 48, 0.5);
      animation: pulse 2s infinite;
    }

    .win-screen h1 {
      font-size: 64px;
      margin: 0 0 20px 0;
      color: #34c759;
      text-shadow: 0 0 20px rgba(52, 199, 89, 0.5);
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    .game-over-screen p, .win-screen p {
      font-size: 28px;
      margin: 10px 0 30px 0;
      color: #ccc;
    }

    .restart-button {
      padding: 15px 40px;
      font-size: 20px;
      background: linear-gradient(145deg, #34c759, #2da84a);
      color: white;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: all 0.3s;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 5px 15px rgba(52, 199, 89, 0.4);
    }

    .restart-button:hover {
      background: linear-gradient(145deg, #2da84a, #34c759);
      transform: translateY(-2px);
      box-shadow: 0 7px 20px rgba(52, 199, 89, 0.6);
    }

    .restart-button:active {
      transform: translateY(0);
    }

    .controls-info {
      margin-top: 20px;
      color: rgba(255, 255, 255, 0.6);
      font-size: 14px;
      text-align: center;
    }

    .controls-info span {
      display: inline-block;
      background: rgba(255, 255, 255, 0.1);
      padding: 5px 10px;
      border-radius: 5px;
      margin: 0 5px;
      font-weight: bold;
      color: #34c759;
    }
  </style>
</head>
<body>
  <div class="game-container">
    <div id="score">Score: 0</div>   

    <div class="box_snake">
      <div id="snake_table"></div>
    </div>

    <div class="controls-info">
      Contrôles: <span>Z</span> <span>Q</span> <span>S</span> <span>D</span>
    </div>
  </div>

  <div id="game_over" class="game-over-screen">
    <h1>Game Over</h1>
    <p id="final_score">Score final: 0</p>
    <button class="restart-button" id="restart_button_gameover">Rejouer</button>
  </div>

  <div id="You_win" class="win-screen">
    <h1>🎉 Victoire! 🎉</h1>
    <p>Vous avez rempli tout le tableau!</p>
    <button class="restart-button" id="restart_button_win">Rejouer</button>
  </div>

  <script src="<%= staticUrl('/javascripts/secret-snake.js') %>"></script>
</body>
</html>
